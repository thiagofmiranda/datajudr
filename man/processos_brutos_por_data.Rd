% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elastic.R
\name{processos_brutos_por_data}
\alias{processos_brutos_por_data}
\title{Search raw processes by filing date range (Elastic \code{processos} index)}
\usage{
processos_brutos_por_data(cfg, data_inicio, data_fim = NULL, ...)
}
\arguments{
\item{cfg}{List. Configuration from \code{\link[=datajud_config]{datajud_config()}} with
\code{fonte = "elastic"} and \code{indice = "processos"}.}

\item{data_inicio}{Character. Start date in \code{"YYYY-MM-DD"} format.}

\item{data_fim}{Character or NULL. End date in \code{"YYYY-MM-DD"} format.}

\item{...}{Additional arguments passed to \code{\link[=datajud_search]{datajud_search()}}.}
}
\value{
A tibble with matching processes.
}
\description{
Filters on \code{dadosBasicos.dpj_dataAjuizamento} (ISO 8601), the reliable date
field for range queries in the raw index (the \code{dadosBasicos.dataAjuizamento}
field is stored as a \code{YYYYMMDDHHMMSS} string).
}
