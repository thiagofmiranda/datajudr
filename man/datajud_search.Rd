% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{datajud_search}
\alias{datajud_search}
\title{Search DataJud and return a tibble of results}
\usage{
datajud_search(
  cfg,
  body,
  page_size = 100,
  max_pages = Inf,
  verbose = TRUE,
  estimate = TRUE
)
}
\arguments{
\item{cfg}{List. Configuration object from \code{\link[=datajud_config]{datajud_config()}}.}

\item{body}{List. Elasticsearch query body from \code{\link[=build_query]{build_query()}}.}

\item{page_size}{Integer. Results per page. Default \code{100}.}

\item{max_pages}{Numeric. Maximum pages to fetch. Default \code{Inf}.}

\item{verbose}{Logical. Print page progress messages.}

\item{estimate}{Logical. Whether to make an extra COUNT request before
fetching to print a download estimate. Only used when \code{verbose = TRUE}.
Default \code{TRUE}.}
}
\value{
A tibble with one row per process.
}
\description{
High-level search function that handles sorting, prints a download estimate,
and parses results into a flat tibble.
}
