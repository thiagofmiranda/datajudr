% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elastic.R
\name{processos_brutos}
\alias{processos_brutos}
\title{Search processes by any field and a list of values}
\usage{
processos_brutos(cfg, variavel, valores, ...)
}
\arguments{
\item{cfg}{List. Configuration from \code{\link[=datajud_config]{datajud_config()}}.}

\item{variavel}{Character. Field to filter on, e.g. \code{"dadosBasicos.numero"}
(raw index), \code{"id_situacao_atual"} (datamart) or \code{"numeroProcesso"}
(public API).}

\item{valores}{Vector. One or more exact values to match. Matches any of them.}

\item{...}{Additional arguments passed to \code{\link[=datajud_search]{datajud_search()}}.}
}
\value{
A tibble with matching processes.
}
\description{
\strong{Primary, recommended function for pulling raw process data.} Generic entry
point that works with \strong{any} source/index: pick a field and pass one or more
exact values. Internally builds a \code{terms} query (logical OR), so a single call
fetches every process whose \code{variavel} matches \strong{any} of \code{valores}. You
choose the field, so no index is assumed — use the raw MTD fields
(\verb{dadosBasicos.*}) with the \code{processos} index, the Datamart fields with the
\code{datamart} index, or the public API fields.
}
\examples{
\dontrun{
cfg <- datajud_config("elastic", indice = "processos",
                      usuario = "user", senha = "pass")
processos_brutos(cfg,
                 variavel = "dadosBasicos.numero",
                 valores  = c("00201597020148140401", "00201597020148140402"))
}
}
\seealso{
\code{\link[=processos_brutos_por_numero]{processos_brutos_por_numero()}} for the single-number shorthand tied
to the raw \code{processos} index.
}
