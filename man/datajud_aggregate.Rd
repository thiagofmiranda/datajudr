% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{datajud_aggregate}
\alias{datajud_aggregate}
\title{Run an aggregation query against DataJud and return tidy results}
\usage{
datajud_aggregate(cfg, body)
}
\arguments{
\item{cfg}{List. Configuration object from \code{\link[=datajud_config]{datajud_config()}}.}

\item{body}{List. Request body from \code{\link[=build_query]{build_query()}} containing an \code{aggs}
clause (see \code{\link[=agg_terms]{agg_terms()}}, \code{\link[=agg_stats]{agg_stats()}}).}
}
\value{
If the body has a single aggregation, a tibble; otherwise a named
list of tibbles, one per aggregation. \code{terms} aggregations return columns
\code{key} and \code{doc_count}; \code{stats} aggregations return their metric columns.
}
\description{
Sends an aggregation request (\code{size} forced to \code{0}, so no hits are returned)
and parses the response buckets into tibbles. Especially useful with the
Datamart source for counting processes by situation, phase, etc.
}
