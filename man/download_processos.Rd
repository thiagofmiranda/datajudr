% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_processos}
\alias{download_processos}
\title{Download DataJud results to Parquet files in batches}
\usage{
download_processos(
  cfg,
  body,
  output_dir,
  page_size = 100,
  batch_pages = 50,
  verbose = TRUE,
  estimate = TRUE
)
}
\arguments{
\item{cfg}{List. Configuration object from \code{\link[=datajud_config]{datajud_config()}}.}

\item{body}{List. Elasticsearch query body from \code{\link[=build_query]{build_query()}}.}

\item{output_dir}{Character. Directory to save Parquet files.}

\item{page_size}{Integer. Results per page. Default \code{100}.}

\item{batch_pages}{Integer. Pages per Parquet file. Default \code{50}.}

\item{verbose}{Logical. Print progress messages.}

\item{estimate}{Logical. Whether to make an extra COUNT request before
downloading to print a download estimate. Only used when \code{verbose = TRUE}.
Default \code{TRUE}.}
}
\value{
Invisibly returns \code{output_dir}.
}
\description{
Paginates through all results and saves them as Parquet files, one file per
batch of pages. Useful for large downloads that exceed memory limits.
}
